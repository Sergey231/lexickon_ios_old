    import XCTest
    @testable import WordsStateRepository

    final class WordsStateRepositoryTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(WordsStateRepository().text, "Hello, World!")
        }
    }
